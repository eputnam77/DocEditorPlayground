import { describe, it, expect } from "vitest";

describe("TipTap heading structure", () => {
  it("should enforce paragraphs after headings", () => {
    expect(false).toBe(true); // Placeholder
  });
});
